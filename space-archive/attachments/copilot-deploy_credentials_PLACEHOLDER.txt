The file 'copilot-deploy_credentials.csv' was detected in the Space but contains sensitive credentials. It has NOT been uploaded to the repository.

Recommended steps to handle the file securely:

1) Store credentials in GitHub Secrets (Settings → Secrets → Actions / Codespaces / Dependabot) or in a secrets manager (AWS Secrets Manager, HashiCorp Vault).

2) If you must keep a repo copy, encrypt it locally with GPG:
   gpg --symmetric --cipher-algo AES256 copilot-deploy_credentials.csv
   # This produces copilot-deploy_credentials.csv.gpg which you may upload safely.

3) Add instructions in your CI to decrypt at runtime using the secret passphrase stored in GitHub Secrets.

DO NOT commit plaintext credential files to the repository.
